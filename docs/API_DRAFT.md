# API Draft

## GraphQL dự kiến

- `searchTrips(from, to, date)`
- `getTripDetail(id)`
- `createBooking(input)`
- `getBookingStatus(code)`
- `askChatbot(message)`

## Service endpoints placeholder

- `GET /health` - trip service
- `GET /health` - booking service
- `GET /health` - ai service
- `GET /` - gateway placeholder

// TODO: Chuẩn hóa schema, input và response khi sang nhánh backend thật.